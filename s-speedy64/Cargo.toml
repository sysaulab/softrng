[package]
name = "s-speedy64"
description = "CLI wrapper for the speedy64 entropy source."
edition = "2021"

[dependencies]
speedy64 = { path = "../speedy64", version = "0.1.0" }
clap = { version = "4.6.7", features = ["derive"] }